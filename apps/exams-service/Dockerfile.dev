# Development Dockerfile with hot reload
FROM node:18-slim

# Install required packages for Prisma
RUN apt-get update && apt-get install -y openssl procps && rm -rf /var/lib/apt/lists/*

WORKDIR /app

# Install nodemon globally for hot reloading
RUN npm install -g nodemon

# Copy package files
COPY package*.json ./
COPY turbo.json ./
COPY apps/exams-service/package*.json ./apps/exams-service/
COPY apps/exams-service/prisma ./apps/exams-service/prisma
COPY packages/ ./packages/

# Install all dependencies (including dev dependencies)
RUN npm install

# Generate Prisma Client for exams-service
WORKDIR /app/apps/exams-service
RUN npx prisma generate

# Go back to root
WORKDIR /app

EXPOSE 3006

# Use start:dev for hot reload - source code will be mounted as volume
CMD ["sh", "-c", "cd apps/exams-service && npx prisma db push && cd ../.. && npm run start:dev --workspace=exams-service"]
